# for online testing (e.g. Tangram Play)
import:
    - https://raw.githubusercontent.com/ENT8R/streetcomplete-mapstyle/master/global.yaml

# for local testing and the final release (e.g. using an emulator) you need to import the file from the url above

sources:
    mapbox:
        type: MVT
        url: https://{s}.tiles.mapbox.com/v4/mapbox.mapbox-streets-v7/{z}/{x}/{y}.mvt
        url_subdomains: [a, b, c]
        url_params:
            access_token: pk.eyJ1IjoiZW50OHIiLCJhIjoiY2pkNGt1cjVwNXpydjM0bGd4ZnprejNtYyJ9.D4uCEBHanButbKh65nX0ZQ
